version: '3.9'

services:
  pytorch:
    container_name: miyoshi_lung_cnn
    build:
      context: .
      dockerfile: ./Dockerfile
    tty: true
    volumes:
      - ./:/src/app
      - /mnt/e/本データ三善用:/src/app/data
      - /mnt/e/本ボクセルデータ:/src/app/voxel_data